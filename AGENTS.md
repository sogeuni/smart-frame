# MoaBom 작업 지침

## 기술 구성

- Android 앱, Kotlin 사용
- 의존성 및 플러그인 버전은 `gradle/libs.versions.toml`에서 관리한다.
- 새 라이브러리는 가능한 Version Catalog에 alias를 추가해 사용한다.

## 코드 규칙

- 기존 패키지 구조와 네이밍을 따른다.
- UI 문자열은 하드코딩하지 않고 `res/values/strings.xml`에 둔다.
- 클래스, 함수에 주석을 한글 및 영문으로 자세하게 단다. 변경 시에도 변경된 부분을 주석에 반영한다.
- 비밀값, API 키, 서비스 설정 파일은 커밋하지 않는다.

## 검증

- 변경 후 다음을 우선 실행한다.
  ```powershell
  .\gradlew.bat assembleDebug
  ```
- 기능 변경에는 가능한 단위 테스트를 추가한다.

## 변경 범위

- 요청과 무관한 리팩터링이나 의존성 버전 변경은 하지 않는다.
- 기존 동작을 깨는 변경은 먼저 영향 범위를 설명한다.
